[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
python-dotenv = ">=1.0.1"
flask = ">=3.1.0"
flask-cors = ">=5.0.0"
openai = ">=1.59.3"
anthropic = ">=0.42.0"

[dev-packages]
requests = ">=2.32.3"
setuptools = ">=75.7.0"

[requires]
python_version = "3.12"
